version: 2
sources:
  - name: 1source  #internalname 
    database: DEMO_DATABASE
    schema: DBT_AADITYA
    tables:
      - name: ORDERS
      - name: cust
      - name: SOMETABLE
      - name: pil_raw
      - name: marketing
       
models:
    - name: pil_raw   #internal name
      columns:
        - name: id
          tests:
            - unique
            - not_null
        - name: gender
          tests:
            - accepted_values:
                arguments:
                  values: ['Male' ,'Female']     
    - name: pil_rawbkup
      columns:
        - name: id
          tests:
            - relationships:
                arguments:
                  to: ref('pil_raw')
                  field: id 
            