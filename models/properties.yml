version: 2

sources:
  - name: 1source  #internalname 
    database: DEMO_DATABASE
    schema: DBT_AADITYA
    tables:
      - name: ORDERS
      - name: cust
      - name: SOMETABLE

      - name: pil_raw
        description: "Raw staging table for person information logs."
        columns:
          - name: id
            tests:
              - unique
              - not_null

          - name: namer
            tests:
              - not_null

          - name: age
            tests:
              - not_null

          - name: email
            tests:
              - not_null

          - name: phone
            tests:
              - not_null

          - name: gender
            tests:
              - not_null
